[project]
name = "amber-tool-trace-stack"
version = "0.1.0"
description = "Package a Python local lab for trace analysis with capacity fixtures, allocation and spill reports, and documented operating limits."
requires-python = ">=3.11"
